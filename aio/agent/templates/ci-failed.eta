# Task: <%= it.issue.title %> - CI Failed

## Issue Details
- **ID**: #<%= it.issue.number %>
- **Title**: <%= it.issue.title %>

## CI Status
The continuous integration checks have failed. Please review and fix the following:

<% if (it.checks && it.checks.length > 0) { %>
## Failed Checks
<% it.checks.forEach(check => { %>
<% if (check.conclusion === 'failure') { %>
### <%= check.name %>
- **Status**: <%= check.status %>
- **Conclusion**: <%= check.conclusion %>
<% if (check.output) { %>
- **Title**: <%= check.output.title %>
- **Summary**: <%= check.output.summary %>
<% } %>

<% } %>
<% }) %>
<% } %>

## Next Steps
1. Review the failed checks above
2. Fix the identified issues
3. Commit and push your changes
4. Wait for CI to pass before proceeding