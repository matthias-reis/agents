# Issue #<%= it.issue.number %>: <%- it.issue.title %>

- State: <%- it.issue.state %>
- Labels: <%= it.issue.labels.length ? it.issue.labels.join(', ') : '—' %>
- URL: <%- it.issue.url %>

## Summary

<% if (it.issue.body.trim()) { %>
<%- it.issue.body.trim() %>
<% } else { %>
_Add details from the issue body here._
<% } %>

## Acceptance Criteria (draft)

- [ ] _Document acceptance criteria here_

## Feedback

_Synced issue + PR feedback will be appended here._

## CI feedback

_CI status summaries will appear here._

## Conflicts

_Conflict details (if any) will be recorded here._

## Links

<% if (it.contextLinks.length) { %>
<% it.contextLinks.forEach(function(link) { %>
- [<%- link %>](<%- link %>)
<% }); %>
<% } else { %>
- _Add helpful context links here_
<% } %>
